# =====================================================================
# Datasets — never committed (images, labels, scrapes, synthetic data).
# Only the pipeline CODE under data/scripts/ is tracked.
# =====================================================================
data/*
!data/scripts/
data/scripts/_label_qa/

# =====================================================================
# Model weights & training runs — large binaries, kept local only.
# Reproduce them with build_dataset.py + train.py.
# =====================================================================
runs/
*.pt
*.onnx
*.pth
*.h5
*.engine
*.tflite

# =====================================================================
# Bulky / generated data formats
# =====================================================================
*.csv
*.zip
*.tar.gz
*.mp4
*.avi
*.mov
datasets/
raw/
temp/

# =====================================================================
# Python
# =====================================================================
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# =====================================================================
# Thesis source & figures — kept out of the code repo. Publish the paper
# via a proper venue (arXiv / Zenodo / university repository), not here.
# =====================================================================
thesislatex/

# =====================================================================
# Secrets & local tooling
# =====================================================================
.env
.claude/
.DS_Store
